package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium SaaS Enterprise Color Tokens
val LightPrimary = Color(0xFF1E40AF)     // Deep Blue / Indigo (#1e40af)
val LightOnPrimary = Color(0xFFFFFFFF)
val LightPrimaryContainer = Color(0xFFDBEAFE) // Beautiful light blue accent container
val LightOnPrimaryContainer = Color(0xFF1E40AF)

val LightBackground = Color(0xFFF8FAFC)  // Clean white-gray (#f8fafc)
val LightSurface = Color(0xFFFFFFFF)     // High-fidelity cards are pure white (#ffffff)
val LightSurfaceVariant = Color(0xFFF1F5F9) // Slate-100 for secondary fields

val LightOutline = Color(0xFFE2E8F0)     // Slate-200 for clean subtle borders (#e2e8f0)
val TextPrimary = Color(0xFF0F172A)      // Slate-900 for dark crisp typography (#0f172a)
val TextSecondary = Color(0xFF475569)    // Slate-600 for secondary metadata (#475569)

// Priority Colors (Linear & ClickUp Inspired)
val PriorityHigh = Color(0xFFEF4444)     // Crimson Red (#ef4444)
val PriorityMedium = Color(0xFFF59E0B)   // Warm Amber Yellow (#f59e0b)
val PriorityLow = Color(0xFF10B981)      // Emerald Green (#10b981)

val PriorityHighContainer = Color(0xFFFEE2E2)
val PriorityMediumContainer = Color(0xFFFEF3C7)
val PriorityLowContainer = Color(0xFFD1FAE5)

// Deep Blue Dark Theme (for consistency or subtle dark fallback if needed)
val DarkPrimary = Color(0xFF3B82F6)
val DarkOnPrimary = Color(0xFF0F172A)
val DarkBackground = Color(0xFF0A0F1D)
val DarkSurface = Color(0xFF131B2E)
val DarkOutline = Color(0xFF1E293B)
